# Example handoff to the on-device agent

> Connect Rebro Frida Pack from this directory to my existing working ReBro baseline. Read AGENTS.md. Use the current Python binding, endpoint 127.0.0.1:27044 and existing working bridge-final.js. Check pack integrity and determine the bridge's actual path from my current workspace/launcher. If local.json exists, verify it without overwriting it unnecessarily. Then use doctor --online, process listing, native/Java smoke on a suitable test process and short native-survey/java-survey. Record full hashes and results. Preserve server, bridge, binding, SELinux and working tmux sessions. Fix only observed pack integration problems. Report which checks actually passed on the device and where their logs are saved. Then use relevant profiles and focused hooks for my subsequent tasks.

This is a sample request for a standalone copy, not authorization to run it now.
For the bundled The Trickster's Pocket skill, follow the outer adapter procedure and existing
trusted pins instead of rediscovering known paths. If the target is known, include
its package/process and expected user action. Reports must reference actual
session.json/summary.json rather than only a console "ready" message.
